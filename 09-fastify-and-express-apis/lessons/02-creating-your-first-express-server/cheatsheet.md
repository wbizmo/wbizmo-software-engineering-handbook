# Lesson 02 Cheatsheet

Install:

    npm install express

Create app:

    const app = express();

Route:

    app.get("/", handler);

Listen:

    app.listen(3000);

JSON:

    res.json(data);

Key takeaway:
    Express makes creating APIs straightforward.
