# Docker & Containerization Module Cheatsheet

Images:
    docker pull
    docker images

Containers:
    docker run
    docker ps
    docker stop

Build:
    docker build

Compose:
    docker compose up
    docker compose down

Volumes:
    docker volume create

Networks:
    docker network create

Production:
    Multi-stage builds
    Non-root users
    Fixed tags
