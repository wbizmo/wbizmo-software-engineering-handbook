# Lesson 06 Cheatsheet

Synchronous:
    Runs line by line.

Asynchronous:
    Some work completes later.

setTimeout:

    setTimeout(() => {}, 1000);

setInterval:

    setInterval(() => {}, 1000);

Stop interval:

    clearInterval(id);

Key takeaway:
    Async JavaScript prevents slow tasks from blocking everything.
